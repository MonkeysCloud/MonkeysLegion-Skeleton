# top‑level keys
app_name    "MyApp"
debug       true

database {
    dsn    "mysql:host=127.0.0.1;port=3306;dbname=myapp;charset=utf8mb4"
    user   "root"
    pass   "secret"
}

# NEW: Toggle template caching on/off
cache {
    enabled false   # set to `true` to enable, `false` to always recompile in memory
}